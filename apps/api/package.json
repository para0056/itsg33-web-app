{
    "name": "itsg33-controls-api",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2022 --outfile=dist/worker.mjs",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {},
    "devDependencies": {
        "esbuild": "^0.24.0",
        "typescript": "^5.5.4",
        "@cloudflare/workers-types": "^4.20240815.0"
    }
}